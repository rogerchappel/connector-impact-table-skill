{
  "name": "connector-impact-table-skill",
  "version": "0.1.0",
  "description": "Prepare local impact tables for planned connector actions",
  "type": "module",
  "bin": {
    "connector-impact-table-skill": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "SKILL.md",
    "docs",
    "examples"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "smoke": "npm run build && node dist/cli.js --help && node dist/cli.js examples/sample.txt --format markdown",
    "validate": "bash scripts/validate.sh"
  },
  "keywords": [
    "agent-skill",
    "cli",
    "local-first",
    "fixtures"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
