# Configuration

V1 is flag-driven so action review remains visible in automation.

- `--format json|markdown` selects deterministic output.
- `--out <path>` writes a review artifact.
- `--fail-on low|medium|high` exits non-zero when a plan meets or exceeds that
  risk level.

Keep connector account IDs and private targets out of public fixtures.
